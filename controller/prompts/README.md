These plain-text system prompts are embedded into the controller binary at build time.
Wording changes require a spec amendment.
Agent placeholders: `{{API_W}}` `{{API_H}}` `{{DISPLAY_W}}` `{{DISPLAY_H}}` `{{MANIFEST}}`; the chat prompt has none.
